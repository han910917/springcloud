spring:
  application:
    name: config-client
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
server:
  port: 8892

management:
  endpoints:
    web:
      exposure:
        include: "*"